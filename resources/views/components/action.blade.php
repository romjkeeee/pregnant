<div class="btn-group">
    @if(isset($edit)) <a href="{{ $edit }}" class="btn-white btn btn-xs"><i class="fa fa-pencil"></i></a> @endif
    @if(isset($delete)) <a href="{{ $delete }}" class="btn-white btn btn-xs"><i class="fa fa-close"></i></a> @endif
</div>
